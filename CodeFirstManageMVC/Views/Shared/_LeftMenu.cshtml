@model IEnumerable<EntityFramework_CodeFirst_Example.Model.ContractType>

@{
    ViewBag.Title = "_LeftMenu";
}

<div class="nav">
    <a class="nav-link" href="@Url.Action("Index", "Home")">
        <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
        Home
    </a>
    <a class="nav-link" href="@Url.Action( "Index", "ContractTypes")">
        <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
        Contract Type
    </a>
    <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseLayouts" aria-expanded="false" aria-controls="collapseLayouts">
        <div class="sb-nav-link-icon"><i class="fas fa-columns"></i></div>
        Layouts
        <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
    </a>
    <div class="collapse" id="collapseLayouts" aria-labelledby="headingOne" data-parent="#sidenavAccordion">
        <nav class="sb-sidenav-menu-nested nav">
            @foreach (var e in Model)
            {
                <a class="nav-link" href="layout-static.html">@e.ContractTypeName</a>
            }
        </nav>
    </div>
    <a class="nav-link" href="@Url.Action( "Index", "Customers")">
        <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
        Customers
    </a>
    <a class="nav-link" href="@Url.Action( "Index", "Payments")">
        <div class="sb-nav-link-icon"><i class="fas fa-tachometer-alt"></i></div>
        Payments
    </a>
</div>

